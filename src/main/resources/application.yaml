server:
  port: 8081
spring:
  application:
    name: service-publisher-1
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
    enabled: true
  instance:
    preferIpAddress: true